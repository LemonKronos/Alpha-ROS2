{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build Debug ROS2",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/install/${input:ros2Package}/lib/${input:ros2Package}/${input:ros2Executable}",
            "args": [
                "--ros-args",
                // "--log-level", "DEBUG",
                // "--log-level", "rcl:=error",
                // "--log-level", "rclcpp:=error"
            ],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "stopAtEntry": false,
            "preLaunchTask": "Build ROS2 single Node Only"
        },
        {
            "name": "Debug ROS2 Container",
            "type": "cppdbg",
            "request": "launch",
            "program": "/opt/ros/jazzy/lib/rclcpp_components/component_container_mt",
            "args": [
                "--ros-args", 
                "-r", "__node:=${input:containerName}"
            ],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "stopAtEntry": false
        }
    ],
    "inputs": [
        {
            "id": "ros2Package",
            "type": "promptString",
            "description": "Enter ROS2 package name",
            "default": "finalize_control"
        },
        {
            "id": "ros2Executable",
            "type": "promptString",
            "description": "Enter ROS2 executable name",
            "default": "finalize_control"
        },
        {
            "id": "containerName",
            "type": "promptString",
            "description": "Enter the name for the ROS 2 container:",
            "default": "alpha_brain"
        }
    ]
}
